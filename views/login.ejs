<!DOCTYPE html>
<html lang="en" dir="ltr">
    <head>
        <%- include('partials/head') %>
        <link rel="stylesheet" href="/css/login.css">
    </head>

    <body>
        <header>
            <%- include('partials/header') %>
        </header>

        <main>
            <form action="/login" method="post">
                <div class="intro">
                    <p class="orizontalSep">
                        In questa pagina puoi effettuare il login per poter
                        accedere alle aree di configurazione e di caricamento
                        dei profili salvati.
                    </p>

                    <div class="elemRow">
                        <label>Nome Utente:</label>
                        <input type="text" name="username" placeholder="Inserisci il nome utente" required>
                        <% if( message.length > 0 ) { %>
                            <div>
                                <%= message %>
                            </div>
                        <% } %>
                    </div>

                    <div class="elemRow orizontalSep">
                        <label>Password:</label>
                        <input class="alignArea" type="password" name="password" placeholder="Inserisci la password" required>
                    </div>
                    <input class="loginButton orizontalSep" type="submit" value="Login">

                </div>
            </form>
        </main>

        <footer>
            <%- include('partials/footer') %>
        </footer>

    </body>
</html>
